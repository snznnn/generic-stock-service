spring:
  application:
    name: generic-stock-service
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    password: mypass
    username: postgres
    hikari:
      schema: stock
  jpa:
    hibernate:
      ddl-auto: create-drop
server:
  port: 8082